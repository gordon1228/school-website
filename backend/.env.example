# Server Configuration
NODE_ENV=development
PORT=5000
CORS_ORIGIN=http://localhost:3000

# Database Configuration (SQL Server)
DB_SERVER=localhost
DB_DATABASE=school_website
DB_USERNAME=sa
DB_PASSWORD=kwang12345
DB_PORT=1433
DB_ENCRYPT=true

# OR Database Configuration (PostgreSQL)
# DATABASE_URL=postgresql://username:password@localhost:5432/school_website

# Session Configuration
SESSION_SECRET=your-super-secure-session-secret-change-in-production
SESSION_NAME=schoolWebsiteSession

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# File Upload
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,application/pdf

# Security
BCRYPT_ROUNDS=12
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_TIME=3600000